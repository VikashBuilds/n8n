name: AgentRouter Daily Login (DISABLED - runs locally via Task Scheduler)

# DISABLED: AgentRouter WAF blocks all data center IPs (GitHub Actions).
# Login runs locally on PC via Windows Task Scheduler instead.
# Script: C:\Users\Vikash Meena\Desktop\Automations\agentrouter-daily-login.ps1
# Schedule: Daily at 6:30 AM IST

on:
  workflow_dispatch:  # Keep manual trigger for reference only

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "This workflow is DISABLED."
          echo "AgentRouter WAF blocks data center IPs (GitHub Actions runners)."
          echo ""
          echo "Login runs locally via Windows Task Scheduler:"
          echo "  Script: agentrouter-daily-login.ps1"
          echo "  Schedule: Daily at 6:30 AM IST"
          echo "  Log: ar_login_history.log"
